# Traversal Policy Module

This module scaffolds the proposal's learned traversal policy.

Current status:

- defines policy-training records
- defines a simple heuristic-compatible policy model interface
- provides path selection helpers over retrieved memory bundles

This is not yet the final imitation-learning or RL policy described in `proposal.md`, but it gives the repository a stable home for that work.
